advisory_id: npm/lactate/GMS-2018-6
datasource_id: gitlab_importer_v2/npm/lactate/GMS-2018-6
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/npm/lactate/GMS-2018-6.yml
aliases:
  - GMS-2018-6
summary: |
  Directory Traversal
  A crafted GET request can be leveraged to traverse the directory structure of a host using the `lactate` web server package, and request arbitrary files outside of the specified web root. This allows for a remote attacker to gain access to arbitrary files on the filesystem that the process has access to read. Mitigating factors: Only files that the user running `lactate` has permission to read will be accessible via this vulnerability.
impacted_packages:
  - purl: pkg:npm/lactate
    affected_versions: vers:npm/>=0.0.0-alpha
    fixed_versions: vers:npm/
severities: []
weaknesses:
  - CWE-1035
  - CWE-937
references:
  - url: https://hackerone.com/reports/296645
    reference_type:
    reference_id:
