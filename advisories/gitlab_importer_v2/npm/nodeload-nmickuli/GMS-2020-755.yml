advisory_id: npm/nodeload-nmickuli/GMS-2020-755
datasource_id: gitlab_importer_v2/npm/nodeload-nmickuli/GMS-2020-755
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/npm/nodeload-nmickuli/GMS-2020-755.yml
aliases:
  - GHSA-wmcq-3wfx-qjx5
  - GMS-2020-755
summary: |
  Directory Traversal in nodeload-nmickuli
  Affected versions of `nodeload-nmickuli` resolve relative file paths, resulting in a directory traversal vulnerability. A malicious actor can use this vulnerability to access files outside of the intended directory root, which may result in the disclosure of private files on the vulnerable system.

  Example request:
  ```
  GET /../../../../../../../../../../etc/passwd HTTP/1.1
  host:foo
  ```


  ## Recommendation

  No patch is available for this vulnerability.

  It is recommended that the package is only used for local development, and if the functionality is needed for production, a different package is used instead.
impacted_packages:
  - purl: pkg:npm/nodeload-nmickuli
    affected_versions: vers:npm/>=0.0.0
    fixed_versions: vers:npm/
severities: []
weaknesses:
  - CWE-1035
  - CWE-937
references:
  - url: https://github.com/JacksonGL/NPM-Vuln-PoC/blob/master/directory-traversal/nodeload-nmickuli
    reference_type:
    reference_id:
  - url: https://www.npmjs.com/advisories/410
    reference_type:
    reference_id:
  - url: https://github.com/advisories/GHSA-wmcq-3wfx-qjx5
    reference_type:
    reference_id: GHSA-wmcq-3wfx-qjx5
