advisory_id: npm/elding/GMS-2017-316
datasource_id: gitlab_importer_v2/npm/elding/GMS-2017-316
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/npm/elding/GMS-2017-316.yml
aliases:
  - GMS-2017-316
summary: |
  Directory Traversal
  `elding` is vulnerable to a directory traversal issue, allowing an attacker to access the filesystem by placing "../" in the url. /nThe files accessible, however, are limited to files with a file extension. Sending a `GET` request to `/../../../etc/passwd`, for example, will return a on etc/passwd/index.js.
impacted_packages:
  - purl: pkg:npm/elding
    affected_versions: vers:npm/>=0.0.0-alpha
    fixed_versions: vers:npm/
severities: []
weaknesses:
  - CWE-1035
  - CWE-937
references:
  - url: https://github.com/JacksonGL/NPM-Vuln-PoC/blob/master/directory-traversal/elding
    reference_type:
    reference_id:
