advisory_id: gem/rack-attack/OSVDB-132234
datasource_id: gitlab_importer_v2/gem/rack-attack/OSVDB-132234
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/gem/rack-attack/OSVDB-132234.yml
aliases:
  - OSVDB-132234
summary: |
  Missing normalization before request path processing
  When using rack-attack with a RoR app, developers expect the request path to be normalized. In particular, trailing slashes are stripped so a request path `/login/` becomes `/login` by the time you're in `ActionController`. Since `Rack::Attack` runs before `ActionDispatch`, the request path is not yet normalized. This can cause throttles and denylists to not work as expected.
impacted_packages:
  - purl: pkg:gem/rack-attack
    affected_versions: vers:gem/<4.3.1
    fixed_versions: vers:gem/4.3.1
severities: []
weaknesses: []
references:
  - url: https://github.com/kickstarter/rack-attack/releases/tag/v4.3.1
    reference_type:
    reference_id:
