advisory_id: gem/activerecord-jdbc-adapter/OSVDB-2013-02-25
datasource_id: gitlab_importer_v2/gem/activerecord-jdbc-adapter/OSVDB-2013-02-25
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/gem/activerecord-jdbc-adapter/OSVDB-2013-02-25.yml
aliases:
  - OSVDB-2013-02-25
summary: |
  SQL Injection
  ActiveRecord-JDBC-Adapter (AR-JDBC) contains a flaw that may allow carrying out an SQL injection attack. The issue is due to the `sql.gsub()` function in `lib/arjdbc/jdbc/adapter.rb` not properly sanitizing user-supplied input before using it in SQL queries. This may allow a remote attacker to inject or manipulate SQL queries in the back-end database, allowing for the manipulation or disclosure of arbitrary data.
impacted_packages:
  - purl: pkg:gem/activerecord-jdbc-adapter
    affected_versions: vers:gem/>=1.2.6|<1.2.8
    fixed_versions: vers:gem/1.2.8
severities: []
weaknesses: []
references:
  - url: https://github.com/jruby/activerecord-jdbc-adapter/issues/322#issuecomment-14053441
    reference_type:
    reference_id:
