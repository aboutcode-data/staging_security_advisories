advisory_id: gem/mapbox-rails/OSVDB-129854
datasource_id: gitlab_importer_v2/gem/mapbox-rails/OSVDB-129854
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/gem/mapbox-rails/OSVDB-129854.yml
aliases:
  - OSVDB-129854
summary: |
  Content Injection via TileJSON attribute
  If you use `L.mapbox.map` or `L.mapbox.tileLayer` to load untrusted `TileJSON` content from a non-Mapbox URL, it is possible for a malicious user with control over the `TileJSON` content to inject script content into the `attribution` value of the `TileJSON` which will be executed in the context of the page using Mapbox.js.
impacted_packages:
  - purl: pkg:gem/mapbox-rails
    affected_versions: vers:gem/>=0.0.0a
    fixed_versions: vers:gem/
severities: []
weaknesses: []
references: []
