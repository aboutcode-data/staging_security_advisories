advisory_id: gem/mapbox-rails/OSVDB-132871
datasource_id: gitlab_importer_v2/gem/mapbox-rails/OSVDB-132871
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/gem/mapbox-rails/OSVDB-132871.yml
aliases:
  - OSVDB-132871
summary: |
  Content Injection via TileJSON Name
  If you use `L.mapbox.map` and `L.mapbox.shareControl`, it is possible for a malicious user with control over the `TileJSON` content to inject script content into the name value of the `TileJSON`. After clicking on the share control, the malicious code will execute in the context of the page using `Mapbox.js`.
impacted_packages:
  - purl: pkg:gem/mapbox-rails
    affected_versions: vers:gem/>=0.0.0a
    fixed_versions: vers:gem/
severities: []
weaknesses: []
references: []
