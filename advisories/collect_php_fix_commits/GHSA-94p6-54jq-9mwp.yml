advisory_id: GHSA-94p6-54jq-9mwp
datasource_id: collect_php_fix_commits/GHSA-94p6-54jq-9mwp
datasource_url: https://github.com/php/php-src
aliases: []
summary: |
  61c8b7851b68aa5e8c2b2cfe9f35ba9107d171be:Fix GHSA-94p6-54jq-9mwp

  Apache only generates REDIRECT_STATUS, so explicitly check for that
  if the server name is Apache, don't allow other variable names.
  Furthermore, redirect.so and Netscape no longer exist, so
  remove those entries as we can't check their server name anymore.

  We now also check for the configuration override *first* such that it
  always take precedence. This would allow for a mitigation path if
  something like this happens in the future.
  48808d98f4fc2a05193cdcc1aedd6c66816450f1:Fix GHSA-94p6-54jq-9mwp

  Apache only generates REDIRECT_STATUS, so explicitly check for that
  if the server name is Apache, don't allow other variable names.
  Furthermore, redirect.so and Netscape no longer exist, so
  remove those entries as we can't check their server name anymore.

  We now also check for the configuration override *first* such that it
  always take precedence. This would allow for a mitigation path if
  something like this happens in the future.
  c1c14c8a0f1067baac1b22474df19266afe21a4d:Fix GHSA-94p6-54jq-9mwp

  Apache only generates REDIRECT_STATUS, so explicitly check for that
  if the server name is Apache, don't allow other variable names.
  Furthermore, redirect.so and Netscape no longer exist, so
  remove those entries as we can't check their server name anymore.

  We now also check for the configuration override *first* such that it
  always take precedence. This would allow for a mitigation path if
  something like this happens in the future.
impacted_packages:
  - purl: pkg:github/php/php-src
    affected_versions:
    fixed_versions:
  - purl: pkg:github/php/php-src
    affected_versions:
    fixed_versions:
  - purl: pkg:github/php/php-src
    affected_versions:
    fixed_versions:
severities: []
weaknesses: []
references:
  - url: https://github.com/php/php-src/tree/48808d98f4fc2a05193cdcc1aedd6c66816450f1
    reference_type: commit
    reference_id: 48808d98f4fc2a05193cdcc1aedd6c66816450f1
  - url: https://github.com/php/php-src/tree/61c8b7851b68aa5e8c2b2cfe9f35ba9107d171be
    reference_type: commit
    reference_id: 61c8b7851b68aa5e8c2b2cfe9f35ba9107d171be
  - url: https://github.com/php/php-src/tree/c1c14c8a0f1067baac1b22474df19266afe21a4d
    reference_type: commit
    reference_id: c1c14c8a0f1067baac1b22474df19266afe21a4d
